{
  "name": "health-mcp-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Single-user Google Health API v4 MCP server on Cloudflare Workers \u2014 read-only, tokens in KV, identity/location redaction.",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "esbuild": "^0.28.2"
  }
}